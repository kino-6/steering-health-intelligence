# 実装のブレークダウン

> **完了(2026-09-02, [docs/287](docs/287_implementation.md))。**
> `scripts/eps_health_recorder.py` に統合、`scripts/demo_recorder.py` で端から端まで実行。
> **実機で8故障中8を検出、遅い側の誤報0。**

> **[docs/225](docs/225_recorder_specification.md) の仕様を、1つの実装にまとめる。**
> 現状、仕様の要素が研究用スクリプトに散っており、**仕様どおりに動く物が存在しない。**

| 仕様の要素 | 今どこにあるか | 状態 |
|---|---|---|
| 個体指紋・逸脱・宣言しない | `scripts/element_v2.py` | ある |
| **同相除去**([docs/286](docs/286_common_mode_results.md)) | `scripts/common_mode.py` | **別スクリプト。要素に入っていない** |
| **遅い時間尺度**([docs/269](docs/269_real_degradation_results.md)) | `scripts/slow_channel.py` | **別スクリプト。要素に入っていない** |
| **チャネルの採否**([docs/280](docs/280_fingerprint_interval_results.md)) | どこにもない | **未実装** |
| **キーオン経過時間・持続**([docs/259](docs/259_spec_revision_persistent.md)) | `element_v2.Record` に欄だけ | **値を入れる処理が無い** |
| 出荷時の掃引・動作点範囲 | `element_v2.take_fingerprint` | 1次元のみ |

---

## T1. 要素を1本にまとめる — `scripts/eps_health_recorder.py`

- [x] **T1-1** 出荷時(EOL)の登録処理。掃引記録 → 指紋一式
  - [x] 同種チャネルの差分を作る(同種が無ければ生のまま)
  - [x] 動作点に対して直線1本を当て、床を求める
  - [x] **別の正常区間で交差検証し、外れるチャネルを宣言から外す**
  - [x] 誤報の設計値から閾値を較正する。**刻めない率は刻めないと返す**
- [x] **T1-2** 運用時の2つの時間尺度を、**互いに独立に**走らせる
  - [x] 速い側: 5秒窓の平均と最大
  - [x] 遅い側: 連続平均。**速い側の発火を待たない**
- [x] **T1-3** 記録の生成。キーオンからの経過時間、キーオフまでの持続を実際に入れる
- [x] **T1-4** 30バイトに詰める。往復して値が戻ることを確認する
- [x] **T1-5** 掃引範囲の外では「宣言しない」を返す

## T2. 端から端まで動かす — `scripts/demo_recorder.py`

- [x] **T2-1** 実インバータの正常運転で登録し、8つの故障を通す
- [x] **T2-2** [docs/286](docs/286_common_mode_results.md) の結果(検出8/8、誤報0)を**再現する**
- [x] **T2-3** 出力を人が読める形で出す。逸脱・粒度・有効性・経過時間

## T3. 仕様との照合

- [x] **T3-1** [docs/225](docs/225_recorder_specification.md) の各行が実装のどこかを対応表にする
- [x] **T3-2** 「主張してはならないこと」7項目を、**出力の欄名で機械的に検査する**
- [x] **T3-3** 実装できていない仕様行を明示する

---

## 実装しないと決めたもの(理由つき)

| 項目 | 理由 |
|---|---|
| 車両側との通信 | AUTOSARの宛先は特定済み([docs/183](docs/183_autosar_capability_channel.md))だが、**車両側が無いので相手が無い** |
| 不揮発への書き込み | 保持期間397日は仕様にあるが、**媒体は部品側の実装事項であり、公開データで検証できない** |
| 許容できる逸脱の下限 | **部品側では決めない**と決定済み([docs/261](docs/261_no_data_decisions_results.md)) |
