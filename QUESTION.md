# 恒久的な問い(閉じない。毎回これに答える)

> **調査している機種(EPS)は、SOTIFに対して何を報告すべきなのか。それは可能なのか。**
>
> ユーザ設定(2026-08-30)。**この問いは archive しない。**
> 下の作業項目が全部閉じたら、**この問いの答えを書き直す。**
> 作業項目に答えることは、この問いに答えることではない([TROUBLES.md](TROUBLES.md) T26)。

## 現在の答え(2026-09-02 時点)

> **報告すべきもの**: その個体が出荷時の自分自身からどれだけ離れたか、その値の粒度、
> そのときの動作点、キーオンからの経過時間、キーオフまで持続したか。
> **どの部品が原因かは報告しない**([docs/276](docs/276_field_mechanism_results.md))。
>
> **検出器は組めた。モデルは組めていない**([docs/291](docs/291_model_gap.md))。
> 検出器は「その個体の基準からどれだけずれたか」を出す。
> **SOTIFが要求するのは「機能がどれだけ足りないか」であり、それは出せていない。**
> 能力値として一度作り、[docs/199](docs/199_pulse_thermal_results.md)・[docs/203](docs/203_cross_machine_replication_results.md) で取り下げてある。
>
> **足りないデータは特定した**: **機能出力(トルク)が内部量と同時に測られていること**(R14)。
> 手元の5データセットは1つも満たさない。**満たす候補は既に見つけてあり、別の理由で保留していた**
> (産業用電動機、電流100 kHz + トルク25.6 kHz、CC BY 4.0、約60 GB)。
>
> **組めた(検出器として):**[scripts/eps_health_recorder.py](scripts/eps_health_recorder.py) に実装し、
> 実機で端から端まで動かした([docs/287](docs/287_implementation.md))。
> **実機の故障8件を8件とも検出し、遅い側の誤報は0である**([docs/286](docs/286_common_mode_results.md), [docs/287](docs/287_implementation.md))。
> **SOTIFでは運用フェーズ(13章)の観測対象として入る**([docs/267](docs/267_sotif_conformance_results.md))。
>
> **機械学習は使っていない。使わないと決めた**([docs/290](docs/290_ml_comparison_results.md))——
> 正常のみで学習する3手法と比べ、**検出は同じ 8/8、記憶容量は86〜19,110倍**、
> 宣言粒度を出す手立ても無いため。**天井にいる課題での比較であり、より難しい課題では見直す。**
>
> **「1 要素の閾値で足りる」ようには見えるが、そうではない**（[docs/302](docs/302_multivariate_results.md)）。
> 立ち上がりの主成分には 6 素子すべてで Vds/Id 以外の量が乗る。ただし検出には効かず、構造の半分は供給電圧＝試験機の量。
> **N=6 の数字はすべて存在証明であり、率ではない。**N を上げる道はコンデンサ 23 個体の記録器事象の特定だけ。
>
> **成立させた鍵は同相除去である。**
> 個体基準を、そのチャネル単体ではなく**同じ個体の同種チャネルとの差分**に対して採る。
> 暖機・周囲温度・電源変動は同種チャネルを同じ向きに動かすので消え、1箇所の故障だけが残る。
> **これを入れる前は誤報75.6%・検出3/8、入れた後は誤報0・検出8/8。**
>
> **まだ言えないこと:**
>
> | | |
> |---|---|
> | 誤報の水準 | **1件/時では動かない。**この登録データで較正できるのは**個体として281件/時**([docs/294](docs/294_unit_level_false_alarm.md)。以前の17.6はチャネル1つの単位で、束ねた個体では16倍粗い)。実装は要求が較正能力より細かいと**どの検出器も動かさない** |
> | 故障の出自 | **人為的に導入した開放・短絡・過温度である。劣化ではない**(R12未達) |
> | 対象 | **EPSではない。**オルタネータ改造のPMSM、1台 |
> | 同種チャネルが無い量 | DCバス電圧は改善せず(16.8倍のまま) |
> | コンデンサ23個体の同期([docs/284](docs/284_method_verification_results.md)) | **同種チャネルが1つしかなく、この手が使えない。未解決** |

---

# 今の作業項目

> **段階が変わった（2026-09-03）。**数件の検出は存在証明として閉じ、**機械学習モデル構築**の観点に移る（[docs/303](docs/303_ml_construction_plan.md)）。

- [x] ML-1 コンデンサ回帰 — docs/306。**個体 LOO では当たる（MAE 0.006〜0.013 / 振幅 0.09）。教師の精度には届かず（0/23）、応力群をまたがない（12.6 倍）**
- [x] ML-2 交絡の処理 — docs/306。**発火±2,000 標本の除去は効果ほぼ無し（相対変化 中央値 0.9%）。**この課題では交絡は支配的でない
- [x] ML-3 非線形は要るか — docs/306。**個体 LOO では要る（18/23 で 10% 以上改善）。ただし群をまたぐと線形より悪い。**1D-CNN は未実施
- [ ] ML-4 C（機能不足）は産業用電動機の取得判断を待つ。**取得は指示があってから**
- [x] ML-5 応力水準 — 未着手のまま残す（下記の方が優先）
- [x] F-1 遅い側の宣言しない・漂流項 — docs/309。**どちらも効かず。効いたのは指紋のスライス（3/6 → 1/6）**
- [x] F-2 B の「何もしない基準」 — docs/310。**S2 成立（16/23）だが基準自体が弱かった**
- [x] N-1a 3 つ目の軸は暦時間か累積ストレスか — docs/312。**累積ストレス。**データは弱く支持（5/6、差 5% 以内）、決めたのは応用の物理（駐車中は劣化しない）
- [x] N-1b 境界 vs 採り直し — docs/315。**どちらも 3/6。二択では解けない。**落ちる 3 素子は漂流と立ち上がりが同じ桁
- [x] N-2 形で分ける — docs/317。**6/6 で誤報 0、水準では落ちる 3 素子を 3 つとも救った。**本物の劣化で検出と沈黙が初めて同時に立った

## 閉じ方の規則

- **`[x]` にできるのは、根拠となる文書番号を書けるときだけ。**
- **「公開データが出れば埋まる」は閉じたことにしない。**問いを部品側で決まる形に裏返す。
- **作業項目が全部閉じたら、必ず上の「現在の答え」を書き直す。**書き直さずに報告しない。

---

# 履歴 — 閉じた問い

## その答えは、実装して動くのか。そしてSOTIFの要求を満たすのか。(2026-09-01 全項目閉)

### M. 実装して動くか

*済* M1 記録器を今の答えのとおりに実装 — docs/264, docs/265。**旧実装は取り下げ済みの能力値を計算していた**(T23)
*済* M2 出荷時の指紋を、掃引記録から実際に生成できるか — docs/265。**実測48バイト。36は誤りだった**
*済* M3 「宣言しない」が仕様どおり働くか — docs/265。範囲外489/489、範囲内0/11
*済* M4 8ビット入力で判定が変わらないか — docs/265。500/500窓で一致
*済* M5 1事象のビット割当 — docs/265。**実測30バイト。27は誤りだった**

### S. SOTIFの要求を満たすか

*済* S1 全13条項の振り分け — docs/267。入る3・入らない9・入れない1
*済* S2 シートと実装の照合 — docs/267。**1行が実装より強い主張をしていた。修正済み**
*済* S3 既知・未知の危険シナリオに対して何をするか — docs/267。**未知は発見できない。発見後の材料である**
*済* S4 受容基準の形に乗るか — docs/267。1件/時 と 0.31 m/s² が規格の例示する2形式に一致

---



## 調査している機種(EPS)は、SOTIFに対して何を報告すべきなのか。それは可能なのか。(2026-08-30 → 2026-09-01 全項目閉)

### A. 何を報告すべきか
  *済* A1 報告の中身。何を出すか — docs/225
  *済* A2 報告してはならないこと。落ちた検証が決めた7項目 — docs/225
  *済* A3 現場で起きている事象の形と合っているか — docs/251, docs/259
  *済* A4 SOTIFのどの活動に対応するのか。機能安全との切り分け — docs/191, docs/153
### B. それは可能か
  *済* B1 ECUが持つ信号で足りるか。必要な測定分解能 — docs/225, docs/261
  *済* B2 記憶と容量で足りるか。指紋・1事象・保持期間 — docs/225, docs/229
  *済* B3 実用的な誤検出率で、意味のある感度が出るか — docs/248, docs/253, docs/261
  *済* B4 出荷時の指紋を現実に採れるか。掃引すべき範囲 — docs/227, docs/263
  *済* B5 標準化された経路に載るか — docs/183
  *済* B6 個体ごとの基準が必要か。必要なら成立するか — docs/232, docs/257
### C. 答えの限界(閉じないことが答えであるもの)
  *済* C1 実機EPSで検証されていない。同型機構の公開データで代替した — docs/224
  *済* C2 断続故障そのものの公開データが無い。仕様は現場の記述から組んだ — docs/238, docs/251
