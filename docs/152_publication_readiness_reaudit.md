# 152. Publication Readiness Re-audit

Date: 2026-08-21
前回監査: [docs/99](99_publication_readiness_audit.md)(2026-07-01)

## なぜ再監査したか

docs/99 以降に **63コミット**が入り、そのすべてが公開データの取得・加工フェーズだった
(NHTSAコホート曲線、comma.ai走行logの兆候抽出、DVSA MOT分母検証、日本データ可用性)。
つまり docs/99 は、このリポジトリの `data/` と `scripts/` が形になる**前**の監査であり、
そのまま公開判断の根拠にはできなかった。

## 実施したスキャン(追跡365ファイル + 未コミット分)

| 検査 | 結果 |
|---|---|
| 秘密鍵・APIトークン実値(`ghp_` / `github_pat_` / `sk-proj` / `sk-ant` / `AKIA` / `BEGIN * PRIVATE KEY`) | 0件。ヒットは docs/99 自身が列挙した検索パターンのみ |
| 本文中のメールアドレス | 0件 |
| ローカル絶対パス(`/Users/` `/home/`) | 0件。ヒットはGM記事URLの `…/home/…` のみ |
| 電話番号形式 | 0件。ヒットはSAE論文番号(`05-2023-2106` 等) |
| 社外秘・機密・NDA | 0件。`機密` は「どのDIDを外部公開するか」という一般的な設計論。`NDA` は HYU**NDA**I / HO**NDA** の誤ヒット |
| キャッシュ/生データの混入 | 0件(`.dvsa_mot` / `.nhtsa_flat` / `.nhtsa_cache` / `.public_log_cache` / `.jp_mlit` はすべて gitignored) |
| `.DS_Store`・500KB超のファイル | 0件 |

## 他者データの再配布(今回の主論点)

**再配布していない。** 取得した元データ約18GBはすべて gitignored であり、
追跡しているのは自分で計算した集計値と、それを再現するスクリプトだけである。

| 種別 | 粒度 | 判定 |
|---|---|---|
| DVSA MOT 由来 3ファイル | 車齢別65行 / メーカー別97行 / グループ×車齢帯12行 | 集計値のみ。個々の検査記録なし |
| NHTSA 由来 5ファイル | コホート別5行 / 判定サマリ27行 / 車種×モード13行 ほか | 集計値のみ。**苦情本文の転記なし** |
| comma.ai 由来 3ファイル | セグメント別938行 | 波形ではなく**算出特徴量**(bias/drift/z値)。元データはMIT |
| `pre_shipment_quality_proxy_*` | 6,000個体 | **完全な合成データ**(`SEED=20260609`)。Kaggle Bosch実データは取得も使用もしていない |

出典・ライセンス・出典表示義務・取得日・派生ファイルの対応は [SOURCES.md](../SOURCES.md) に整理した。
OGL v3.0(DVSA)と MIT(comma.ai)は**出典表示が義務**であり、規定の帰属文を記載済み。

## 今回是正したこと

1. **Git履歴の個人メール**(docs/99 が唯一「未解決」としていた項目)
   69コミットの author / committer に個人Gmailが残っていた。`git filter-branch --env-filter` で
   noreply アドレスへ置換し、`refs/original` を削除、reflog expire + gc を実施。
   全refで残存0件。書き換え後もツリーは同一(`16c7c48f…`)、コミット数138も不変。
   再発防止として global の `user.email` も noreply に変更した。

2. **一人称の company wording**
   `自社` 93件 / `当社` 6件を `サプライヤ` へ、`社内` 127件を `サプライヤ内` へ置換(計116ファイル)。
   内部事実の混入ではない([AGENTS.md](../AGENTS.md) 1条は守られており該当0件)が、
   **個人研究が所属組織の業務成果物と誤読される**表現だったため是正した。
   初出は最初のコミット(2026-05-23)で、最上位ルール確定(2026-07-07)後も書き足されていた。
   1条が禁じるのは「内部情報を使うこと」であって文体ではなく、掃除の対象外だったことが原因。

3. **公開向けREADMEとSOURCES.md**
   READMEを「結論を先に、根拠を後に」の構成へ書き直し、索引を [docs/INDEX.md](INDEX.md) へ分離。

## 残る既知事項

- force push 前の旧コミットは、GitHub側でGCされるまで直接SHA指定でアクセスされうる。
  ただしこのリポジトリはfork 0件・一度も公開されておらず、旧SHAはどこにも露出していない。

## 判定

> 公開可能。前回監査の条件3件のうち、未解決だった「Git履歴の個人メール」は解消済み。
