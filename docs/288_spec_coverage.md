# 288. 仕様38行を実装と突き合わせた — 21行は実装、13行は測定値、4行は見送り

[TASKS.md](../TASKS.md) の T3 を実施した。**追加取得ゼロ。**

> **T3には `[x]` を付けていたが、実際には終わっていなかった。**
> [docs/287](287_implementation.md) に書いた対応表は7行で、**仕様38行のうち31行を見ていなかった。**
>
> **突き合わせた結果、未分類は0である。**そして**分類を機械が強制するようにした。**

実行: `python3 scripts/check_spec_coverage.py`

## 分類

| 状態 | 行数 | 意味 |
|---|---|---|
| **実装あり** | **21** | `scripts/eps_health_recorder.py` に対応する記号がある |
| **測定値** | **13** | 仕様が述べる数値であり、動かすコードではない |
| **見送り** | **4** | 実装しないと決めた。理由を記録している |

## 見送った4行

| 行 | 理由 |
|---|---|
| 不揮発への書き出し | **媒体は部品側の実装事項であり、公開データで検証できない** |
| 保持期間(397日) | 同上。**要件としては仕様に残る** |
| ECU内部信号の観測床 | **分解能では決まらないと確定**([docs/261](261_no_data_decisions_results.md))。アナログ雑音の設計項目 |
| 許容できる逸脱の下限 | **部品側では決めないと決定**([docs/261](261_no_data_decisions_results.md)) |

## 機械で強制する2つ

**どちらもコミット時に走り、実際に止まることを確認した。**

| 検査 | 何を止めるか | 確認した動作 |
|---|---|---|
| **spec coverage** | 仕様に行を足して分類しないこと | 未分類の行を1つ足す → **コミットが止まる** |
| **forbidden output** | 主張しないと決めた語を、要素の欄名に入れること | `capability_percent` という欄を足す → **コミットが止まる** |

> **7つの「主張してはならないこと」は、これまで文章だった。**
> **能力値を計算するコードが取り下げ後も残っていた**([TROUBLES.md](../TROUBLES.md) T23)のは、
> **文章がコードを止められなかったからである。**
> **今は止まる。**

**そして仕様は、実装より先に伸びられなくなった。**
[docs/225](225_recorder_specification.md) に行を足したら、実装するか、測定値と宣言するか、
見送ると書くかを決めるまでコミットできない。

## この検査が保証しないこと

- **記号の存在だけを見ている。**`common_mode_reject` という名前があることは確かめるが、
  **それが正しく動くことは確かめない。**それは [docs/287](287_implementation.md) のデモの役目である
- **禁止語は語の照合である。**別の名前で同じ主張をする欄は止められない
- **測定値13行は、値が今も正しいかを見ていない。**それは `sheet currency` と訂正リンクの役目である

## Rule Check

- **`[x]` を付けていたT3が終わっていなかったことを、冒頭に書いた**
- **31行を見ていなかったという数を出した**
- **2つの検査が実際に止まることを、壊して確かめてから書いた**
- **検査が保証しないことを3つ挙げた**
